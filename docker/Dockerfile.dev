ARG NODE_IMAGE=node:20.19.6-alpine
ARG APP_PORT=3000

# =====================================
FROM ${NODE_IMAGE}

WORKDIR /home/app

RUN apk update && apk upgrade && apk add --no-cache bash git

EXPOSE $APP_PORT
