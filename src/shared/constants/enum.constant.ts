export enum FriendEnum {
  accepted = 'accepted',
  declined = 'declined',
  pending = 'pending',
}

export enum ConservationEnum {
  normal = 'normal',
  secret = 'secret',
}

export enum MessageTypeEnum {
  text = 'text',
  image = 'image',
  video = 'video',
  another = 'another',
}
