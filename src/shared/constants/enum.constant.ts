export enum FriendEnum {
  accepted = 'accepted',
  declined = 'declined',
  pending = 'pending',
}

export enum ConservationEnum {
  normal = 'normal',
  secret = 'secret',
}

export enum MessageTypeEnum {
  text = 'text',
  image = 'image',
  video = 'video',
  another = 'another',
}

export enum MessageEncryptTypeEnum {
  one = 1,
  three = 3,
}

export enum VerifyRequestStatusEnum {
  pending = 'pending',
  expired = 'expired',
  done = 'done',
}
